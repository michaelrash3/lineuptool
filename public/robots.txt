# Coach's Card — allow all crawlers. The app shell lives at / (the rest of the
# app renders client-side behind Firebase auth, so there are no extra static
# paths to expose). Public Tryouts Portal links use per-team dynamic slugs that
# coaches share directly, so they aren't enumerated here.
User-agent: *
Allow: /

# Add a Sitemap directive once a production domain is configured, e.g.:
# Sitemap: https://your-domain.example/sitemap.xml
