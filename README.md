# lineuptool

Baseball Lineup Generator

## Firestore rules

The repository contains deployable Firestore rules in `firestore.rules`.

Use `docs/firebase-rules-rollout.md` for:

- CLI setup
- deploy steps
- emulator validation matrix
- rollback procedure
